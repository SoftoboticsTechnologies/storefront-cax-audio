# Changelog

Concise history. Not a full commit log — one line per meaningful change.

## 2026-09-24
- Mobile responsiveness pass (audited in headless Chrome at 320/360/375/414/768/1024/1280 px — no horizontal overflow on home, collection, product, search, cart, checkout, auth, contact, account): header gets a search icon below md (`navbar/mobile-search.tsx`, opens the existing `SearchOverlay`); logo scales `h-8`→`h-9` (≥360px)→`h-10` (sm)→`h-12` (md); "Sign in" is a user icon below sm (label kept as `sr-only`). Collection/search product grid is 2-up on phones (`grid-cols-2 lg:grid-cols-3`, tighter gap), skeleton to match. Language/currency/theme pickers removed from the header row (user edit).
- Header category bar (desktop + mobile): dropdowns are back, now showing the **full** Vendure collection tree under each top-level collection (all levels, indented), led by a "View all <name>" link (`Navigation.viewAllIn`, 4 locales). New `getCollectionTree` in `features/collections/data.ts` + client-safe `features/collections/collection-tree.ts`. Supersedes the earlier same-day "top-level links only" change.
- Homepage `CategoryDiscovery` tiles: top-level collections only (`getRootCollections`), so model collections (BMW 1 Series, Mercedes-Benz A-Class) no longer appear; names trimmed for display.
- Header category bar (desktop + mobile): model dropdowns removed; only top-level collections (makes/categories) are shown, as plain links. Names are `.trim()`med for display. Unused `Navigation.viewAll` key dropped (4 locales).
- Removed unreferenced assets: `public/hero/{Audi,Mercedes,Porsche,bmw,hero 1st}.png` (~9.5 MB of source PNGs; the `.webp` versions are what's used), 3 unused `public/logo/DRIPFUNNEL *.png` variants, `public/logo/cax audiologo 2.png`, `public/next.svg`, `public/vendure.svg`.
- Homepage editorial banner: fixed a bright seam on hover — the 105% zoom spilled the photo past the navy fade into the text column; the photo wrapper is now `overflow-hidden`.
- Homepage brand grid: removed the "N products" / "Coming soon" line under each make; tiles are logo + name only. `Home.brands.productCount`/`comingSoon` keys dropped in all 4 locales.
- Homepage editorial banner: Mercedes-Benz collection now shows a site-curated E-Class (W213) interior photo (`public/banners/mercedes-interior.webp`, Wikimedia Commons CC BY 2.0, credits in `public/banners/CREDITS.txt`) instead of its Vendure featured asset. Slug-keyword map `BANNER_IMAGES` in `site/home/editorial-banner.tsx`; unmatched collections still use their featured asset.
- Dev/preview port changed from 3001 to 1001 (`package.json` scripts, `.env.example`, README).

## 2026-09-23
- Collection hero + homepage editorial banner, mobile (<md): the photo is now the full-card background behind the text with a `bg-navy/75` overlay, instead of a strip stacked under the text. md+ keeps the split layout.
- Collection pages: the plain `h1` header is replaced by a navy split banner in the homepage editorial style (`features/collections/routes/collection-hero.tsx`): eyebrow, collection name (h1), Vendure description as plain text (fallback `Collection.heroFallbackDescription`), "Browse products" button scrolling to `#products`, featured asset on the right. Applies to every collection (all car makes/models and categories). Fallback photo map moved from `site/home/category-images.ts` to `features/collections/fallback-images.ts` so the feature can use it; the loading skeleton gained a matching banner placeholder.
- Fix React 19 dev warning "Encountered a script tag while rendering React component" from next-themes 0.4.6: `site/providers/theme-provider.tsx` passes `scriptProps={type: 'application/json'}` on the client only, so the server-rendered pre-paint theme script still runs and the inert client copy isn't flagged.
- Hero carousel: Cax Audio brand banner (`public/hero/hero 1st.png` → `cax-audio.webp`) added as the first slide, linking to `/search` (slides without `match` link to the catalog), with `Hero.slides.brand` copy in all 4 locales. Arrows are now vw-sized (`size-[3.3vw]`, `left/right-[1.5vw]`) to stay clear of the artwork's left-hand text, and hidden below md (swipe/dots there).
- Hero carousel artwork replaced: 4 banners (BMW, Mercedes-Benz, Audi, Porsche) from the PNGs in `public/hero`, converted to 1920x649 WebP (~100-140 KB each vs ~2 MB PNG); Porsche slide + `Hero.slides.porsche` copy added in all 4 locales (links to `/search?q=Porsche` until a `porsche` collection exists).
- Homepage: `EditorialBanner` moved from after Featured products to after `BenefitsSection` ("The smarter way to upgrade your car"), before the CTA band.
- Hero carousel: each slide now has its own caption band under the banner (navy; eyebrow, display title, description, orange "Shop <make>" button), copy in `Hero.slides.<bmw|mercedes|audi>` in all 4 locales. Arrows/dots are pinned over the image (arrows at `top-[25vw]`, below the artwork's side text; image height = 33.8vw).
- Homepage hero is now a full-width banner carousel (`site/home/hero-carousel.tsx` on the shadcn/Embla `Carousel`): `public/hero/1-3.avif` (BMW, Mercedes-Benz, Audi; 1920x649, headline baked into the artwork), loops, autoplays every 3.5 s (pauses on hover/focus, off for reduced motion), left/right arrows + dots. Slides link to the collection whose slug matches (`site/home/hero-slides.ts`, resolved at build time from `getTopCollections`), else `/search?q=<make>`. The text headline stays as an `sr-only` h1. Replaces the text hero + faded collection backdrop.
- Homepage trust strip: bigger cards — round orange-tint icon, bold title + one-line description (new `Home.trust.*Description` keys in all 4 locales), soft orange shadow and decorative corner arcs.
- Homepage category tiles: collections without a Vendure featured asset get a fallback photo (`site/home/category-images.ts`, matched on slug keywords → `public/categories/*.jpg`, Wikimedia Commons CC BY-SA, credits in `public/categories/CREDITS.txt`). A featured asset uploaded in Vendure always takes precedence.
- Footer layout: four columns split by hairlines on lg+ (brand · Categories · Customer · Contact), Bebas headings with a short orange underline, larger text/icons; Contact shows address, phone, WhatsApp number and email; a Google Business Profile link sits under the social icons in the brand column (`components/social-icons.tsx` glyphs). Social buttons (Facebook/Instagram/YouTube/LinkedIn) always shown; each becomes a link once its URL is set in `BUSINESS.social` (all empty now, so static icons). Credit-bar divider inset to the container.
- Footer: thin credit bar at the bottom of the footer (same navy background, divider line) (copyright left, "| Powered by" + full-colour DripFunnel logo `public/logo/Dripfunnel-logo.png` → dripfunnel.com right); replaces the old bottom row (copyright + Powered by Vendure & Next.js). `sm:pr-20` keeps it clear of the floating WhatsApp button.
- Business contact details (`config/business.ts`): "Call us" in announcement bar, footer Contact column (replaces Vendure links), floating WhatsApp button, new `/contact` page with an enquiry form that hands off to WhatsApp/email (no backend), LocalBusiness JSON-LD site-wide. Translations in all 4 locales.
- Header: only the category bar is sticky now (moved out of `<header>` to a sibling `<nav>`); logo/search row and announcement bar scroll away.
- Renamed site to "Cax Audio": `NEXT_PUBLIC_SITE_NAME` in `.env.local`/`.env.development`/`.env.production` (was `Cox-Audio`), `SITE_NAME` default in `config/metadata.ts`, footer copyright now `{siteName}`-parameterized and footer description rewritten (all 4 locales). API/deploy `dripfunnel.com` URLs unchanged.
- Branding: Cax Audio logo (`public/logo/cax audiologo.png`, used as-is at the owner's request, ~1MB) in header/footer; favicon set from `public/favicon/` (`src/app/favicon.ico` replaced, PNG/apple/manifest linked in `site/locale-layout.tsx` metadata; `favicon.svg` intentionally unlinked — 1.4MB); `site.webmanifest` name/paths fixed.
- Homepage brand grid: car-make logos (Simple Icons CC0, `public/brands/`) and zero-product makes with a known logo (e.g. Mercedes-Benz → "Coming soon"); new additive `FacetByCodeQuery`. Hero vehicle-finder form commented out (kept for future use).
- Visual redesign modeled on autodukan.com: brand tokens (orange `#ff6633` primary, navy ink/bands, Barlow + Bebas Neue), `SectionHeading` primitive, two-tier sticky header (removed `mt-16` fixed-header offsets from 9 routes), navy announcement bar/footer, homepage recomposed (hero vehicle finder, trust strip, category tiles, brand grid from the `vehicle-brand` facet, navy "why us"), product card restyle. Additive only on the data side: `CatalogFacetValuesQuery` + `getCatalogFacet`, `getRootCollections`, `FeaturedProducts` slug now optional. No cart/checkout/pricing/currency/auth logic touched. Removed `site/home/shop-by-category.tsx` (merged into the category tile grid). See `docs/ui-system.md`.

## 2026-09-04
- CI: enabled, then same-day disabled (commented out), the CloudFront cache-invalidation step in the S3 deploy workflow (`.github/workflows/deploy-s3.yml`).

## 2026-09-03
- Shipment tracking: added tracking fields to `features/account/graphql.ts`, surfaced on `account/routes/orders/order-detail.tsx`; translations added across `en`/`de`/`hi`/`tel`. Minor related changes to `checkout-flow.tsx`/`shipping-address-step.tsx`.
- Static-export migration (superseding the 2026-09-02 SSR→SSG/ISR entry below — `partialPrefetching`/`cacheComponents` were removed): `next.config.ts` switched to `output: 'export'` (full static export, S3+CloudFront, no Node/server runtime), `trailingSlash: true`, `images.unoptimized: true`. Cart, checkout, auth, and currency resolution moved from Server Actions/cookies to client-side calls against Vendure directly (`platform/vendure/channel-client.ts`). `product/[slug]` became `product/[slug]/[[...variant]]` (optional catch-all for a variant SKU) so each non-default variant gets its own statically prerendered page with a real baked-in price (`features/products/data.ts#getProductVariantParams`). Introduced `features/search/search-params-sync.tsx` so no component calls `next/navigation`'s `useSearchParams()` directly; `collection-results.tsx`/`product-card.tsx`/`product-info.tsx`/`product-price-client.tsx` updated to take build-time initial data superseded by a live client fetch. Root redirect and `scripts/write-root-redirect.mjs` updated for trailing-slash static hosting compatibility. `next/link` prefetch disabled globally to work around a Next.js static-export bug. See `docs/decisions.md` for the full rationale; `docs/architecture.md`'s "Rendering strategy" section documents the resulting model.
- Env/deploy: updated `.env.production` and `.github/workflows/deploy-s3.yml` for static-site-generation output and S3 hosting.

## 2026-09-02
- SSR → SSG/ISR for catalog routes (product/collection): added `generateStaticParams` to `features/products/routes/page.tsx` (backed by new `features/products/data.ts` `getPopularProductSlugs`, reusing the existing `SearchProductsQuery` — no new GraphQL op) and `features/collections/routes/page.tsx` (backed by existing `getTopCollections`). Enabled `partialPrefetching: true` in `next.config.ts` alongside the already-on `cacheComponents`. Known slugs now serve as fully static/ISR pages (`x-nextjs-prerender: 1` verified against a production build/start); unknown slugs still render via the existing route-level `loading.tsx` App Shell and upgrade to static after their first visit. Cart, checkout, account, auth, sign-in, and search intentionally do **not** get `generateStaticParams` and remain dynamic per `docs/decisions.md` — they depend on cookies/Server Actions that cannot be prerendered. Currency-dependent pricing is unaffected: `getActiveCurrencyCode()` still resolves per request and streams into the prerendered shell exactly as before. Verified with `check-types`, `lint`, `test` (3 pre-existing Windows-only upgrade-tooling test failures, unrelated, reproduced on `main` before this change), and a full `next build` + `next start` (standalone) smoke test against real product/collection slugs.

## 2026-08-31
- Homepage redesign (task 2 of the UI redesign): recomposed products-as-hero, editorial homepage — `AnnouncementBar` (site-wide) → visual `Hero` (real collection image/name, no fabricated shipping claim) → `CategoryDiscovery` (quick-nav strip) → `FeaturedProducts` → `EditorialBanner` → `TrendingProducts` (sourced from a second real collection, no fake "new" sort) → `ShopByCategory` (incl. subcategories) → `BenefitsSection` (restyle of prior "Why Shop With Us") → `CtaBanner` (promotional, links to `/register`, no email form — no subscription backend exists). `GetTopCollectionsQuery` top-level items extended with `featuredAsset`/`description` (additive). `features/products/featured-products.tsx` generalized to accept `collectionSlug` (was hardcoded `"electronics"`) and now exports both `FeaturedProducts` and `TrendingProducts`. No commerce/cart/checkout/auth logic touched; `check-types`, `lint`, and `build` all pass; manually verified EN/DE homepages render all sections with correct translations.
- Docs: created `CLAUDE.md`, `docs/commerce.md`, `docs/ui-system.md`, `docs/decisions.md`, `docs/changelog.md` as persistent project memory ahead of the premium-ecommerce UI modernization (existing `docs/architecture.md` and `docs/adr/` kept as-is). No source changes.
- Fixed faceted search: multi-select within one facet group now uses OR (was incorrectly AND'ing all selected values across groups, producing zero results).
- Header/navigation modernization (task 1 of the UI redesign): extended `GetTopCollectionsQuery` with one level of `children`/`featuredAsset`; desktop `navbar-collections.tsx` now renders a mega-menu dropdown for collections with children (plain link preserved for leaf collections); mobile `mobile-nav.tsx` mirrors this with an accordion. Added a shared `search-overlay.tsx` (command palette + live product suggestions via a new `getSearchSuggestions` server action reusing the existing `SearchProductsQuery`, no new GraphQL operation) wired into both desktop `search-input.tsx` and the mobile nav's search trigger. No commerce/cart/checkout/auth logic touched; `check-types`, `lint`, and `build` all pass.

## 2026-08-29
- Fixed stale-USD bug: `getActiveCurrencyCode` now validates the currency cookie against `channel.availableCurrencyCodes`, falling back to channel default.
- Implemented Stripe Elements checkout end-to-end (PaymentIntent creation, client-side card collection, webhook-driven settlement); added `PaymentProcessingBanner` for the async confirmation window; hardened cart mutations to auto-recover from `ORDER_MODIFICATION_ERROR`.
- Fixed storefront Docker build (Next.js `output: 'standalone'`) and product image 404s.
